# MongoDB Atlas 连接字符串
# 格式: mongodb+srv://用户名:密码@集群地址/数据库名?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# JWT 密钥（生产环境请使用强随机字符串）
# 可以使用 Node.js 生成: require('crypto').randomBytes(64).toString('hex')
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# JWT 过期时间（可选，默认 7d）
# 格式: 数字+单位，如 7d, 24h, 60m, 3600s
JWT_EXPIRE=7d

# 服务器端口（可选，默认 3000）
PORT=3000

# 环境变量（development/production）
NODE_ENV=development

# 创建超级管理员，修改后运行npm run create:super-admin
SUPER_ADMIN_USERNAME=admin
SUPER_ADMIN_EMAIL=admin@fuyou.com
SUPER_ADMIN_PASSWORD=MySecurePass123
